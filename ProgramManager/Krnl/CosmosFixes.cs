using System;
using System.Collections.Generic;
using System.Text;

namespace ProgramManager
{
    public static class CosmosFixes
    {
        /*
        public static string[] splt(string text, char separator)
        {
            string[] result = new string[0];
            int count = 0;
            int[] indexes = new int[text.Length];
            int ii = 0;
            for(int i = 0; i < text.Length; i++)
            {
                char extracted = char.Parse(text.Substring(i, 1));
                if(extracted == separator)
                {
                    count++;
                    indexes[ii] = i;
                    ii++;
                }
            }

            result = new string[count];

            for(int i = 0; i < result.Length - 1; i++)
            {
                result[i] = text.Substring(indexes[i], indexes[i + 1] - indexes[i]);
            }
            result[count - 1] = text.Substring(indexes[count - 1]);
            return result;
        }
        */
    }
}
